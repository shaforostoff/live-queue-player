<?xml version="1.0" encoding="utf-8"?>
<resources>

  <style name="app_theme" parent="@android:style/Theme.Light.NoTitleBar">
    <item name="android:background">@color/background</item>
    <item name="android:colorForeground">@color/foreground</item>
    <item name="android:textColor">@color/foreground</item>
    <item name="android:textColorPrimary">@color/foreground</item>
    <item name="android:textColorSecondary">@color/foreground</item>
    <item name="android:buttonStyle">@style/buttonStyle</item>
    <item name="android:textSize">25sp</item>
    <item name="android:gravity">center</item>
    <item name="android:alertDialogTheme">@android:style/Theme.DeviceDefault.Light.Dialog.Alert</item>
    <item name="android:listViewStyle">@style/listViewStyle</item>
  </style>

  <!-- Theme.Light's default listSelector is the legacy orange pressed-state gradient; rows
       provide their own feedback, so suppress it for every list in one place. -->
  <style name="listViewStyle" parent="@android:style/Widget.ListView">
    <item name="android:listSelector">@android:color/transparent</item>
  </style>

  <style name="buttonStyle" parent="@android:style/Widget.Button">
    <item name="android:padding">10dp</item>
    <item name="android:textColor">@color/foreground</item>
    <item name="android:background">@color/buttonBackground</item>
  </style>
</resources>